PROJECT(SOLVER CXX)
CMAKE_MINIMUM_REQUIRED(VERSION 3.12)

ADD_EXECUTABLE(main main.cpp)

ENABLE_TESTING()
ADD_TEST(NAME main COMMAND ${EXECUTABLE_OUTPUT_PATH}/main
         WORKING_DIRECTORY ${PROJECT_BINARY_DIR})
         